<Page x:Class="DbSyncTool.Views.Pages.SqlToolPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="SQL工具">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题 -->
        <TextBlock Grid.Row="0" Text="SQL 工具" FontSize="20" FontWeight="Bold" Margin="0,0,0,16"/>

        <!-- 数据库选择 + 执行按钮 -->
        <Grid Grid.Row="1" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="目标数据库：" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <ComboBox Grid.Column="1" x:Name="CmbDatabase"
                      DisplayMemberPath="DisplayName"
                      Height="32" Margin="0,0,12,0"/>
            <Button Grid.Column="2" x:Name="BtnExecute" Content="▶ 执行"
                    Height="32" Padding="16,0" Margin="0,0,8,0"
                    Click="BtnExecute_Click"/>
            <Button Grid.Column="3" x:Name="BtnClear" Content="清空结果"
                    Height="32" Padding="12,0"
                    Click="BtnClear_Click"/>
            <TextBlock Grid.Column="4" x:Name="TxtStatus"
                       VerticalAlignment="Center" Margin="12,0,0,0"
                       Foreground="#718096" FontSize="12"/>
        </Grid>

        <!-- SQL输入框 -->
        <Border Grid.Row="2" BorderBrush="#CBD5E0" BorderThickness="1" CornerRadius="4" Margin="0,0,0,8">
            <TextBox x:Name="TxtSql"
                     Height="120" AcceptsReturn="True" TextWrapping="Wrap"
                     FontFamily="Consolas" FontSize="13" Padding="8"
                     VerticalScrollBarVisibility="Auto"/>
        </Border>

        <!-- 结果DataGrid -->
        <Border Grid.Row="3" BorderBrush="#CBD5E0" BorderThickness="1" CornerRadius="4">
            <DataGrid x:Name="ResultGrid"
                      AutoGenerateColumns="True"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      IsReadOnly="True"
                      SelectionMode="Extended"
                      GridLinesVisibility="Horizontal"
                      AlternatingRowBackground="#F7FAFC"
                      HorizontalScrollBarVisibility="Visible"
                      VerticalScrollBarVisibility="Auto"
                      FontSize="12"/>
        </Border>

        <!-- 底部操作栏 -->
        <Grid Grid.Row="4" Margin="0,8,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" x:Name="TxtRowCount"
                       VerticalAlignment="Center"
                       Foreground="#718096" FontSize="12"/>
            <Button Grid.Column="1" x:Name="BtnExport" Content="导出CSV"
                    Height="32" Padding="12,0"
                    Click="BtnExport_Click"/>
        </Grid>
    </Grid>
</Page>
